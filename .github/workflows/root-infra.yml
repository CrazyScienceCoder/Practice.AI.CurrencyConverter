name: Root · Infra

on:
  pull_request:
    branches: [develop, main]
    paths:
      - '*.md'
      - '*.ps1'
      - 'docker-compose*.yml'
      - 'keycloak/**'
      - '.github/workflows/*.yml'
      - 'docs/**'

jobs:
  build-and-test:
    name: Build & Test
    runs-on: ubuntu-latest
    steps:
      - run: echo "Root/infra change, no service build needed"